import { Command } from '../registry';

/**
 * Planned commands that are not yet implemented.
 * All previously planned commands have shipped — this list is intentionally empty.
 */
export const plannedCommands: Command[] = [];
